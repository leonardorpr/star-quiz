import styled from 'styled-components';

export default c => styled(c)`
  .logo {
    height: 35px;
    width: 35px;
  }

  .letter-logo {
    margin-left: 10px;
    color: rgba(255, 255, 255, 0.5);
    font-weight: bolder;
  }
`;
